#include "main.h"
#include <stdio.h>

/**
 *  _strlen - returns the length of a string.
 * @s: var to be used
 * Return: Always 0.
 */
int _strlen(char *s)
{
	int i;

	while (s[i] != '\0')
		i++;
	return (0);
}
